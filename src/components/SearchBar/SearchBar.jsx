import PropTypes from 'prop-types';

const SearchBar = () => {
  return (
        <div>
            <span>
                <input type='text' />
            </span>
        </div>
  );
};

SearchBar.propTypes = {

};

export default SearchBar;
